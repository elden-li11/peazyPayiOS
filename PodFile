# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'peazyPayiOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for peazyPayiOS
  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
  pod 'FirebaseUI'
  pod 'Firebase/Auth'

  target 'peazyPayiOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'peazyPayiOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
